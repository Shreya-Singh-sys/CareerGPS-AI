# 1. Dependency folders (Sabse heavy)
node_modules/
jspm_packages/

# 2. Environment variables (Most Important - Isme aapki API keys hain!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 3. Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# 4. OS related files
.DS_Store
Thumbs.db

# 5. IDE settings
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# 6. Build/Dist folders (Agar aap TS ya build tools use kar rahe hain)
dist/
build/

# 7. Uploads (Agar user ki resumes locally save kar rahe ho)
uploads/